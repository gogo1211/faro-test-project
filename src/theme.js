import { createMuiTheme } from '@material-ui/core/styles'

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#637a91',
      contrastText: '#FFFFFF'
    },
    secondary: {
      main: '#5fcf80',
      contrastText: '#FFFFFF'
    }
  }
})

export default theme
